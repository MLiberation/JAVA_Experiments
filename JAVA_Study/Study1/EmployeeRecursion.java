import winter2026.lab1.Employee;
public class EmployeeRecursion 
{
    public static Employee findByName(Employee[] employees, String name, int index) 
	{
		if(this == employee) return true;
		if(employee == null) return false;
		if(getClass() != employees.getClass())return true;
		
			Employee other = (Employee) otherObject;

		if(this.name == null)
		{
			return other.name == null;
		}
		else
		{
			return this.name.equals(other.name);
		}
    }

    public static String reverseString(String str) 
	{
		if(str != null) return true;

		char letter = str.charAt(str.length-1);
    }

    public static void main(String[] args) 
	{
        Employee[] staff = new Employee[5];

        staff[0] = new Employee("Alice", 120000);
        staff[1] = new Employee("Bob", 100000);
        staff[2] = new Employee("Charlie", 80000);
        staff[3] = new Employee("David", 150000);
        staff[4] = new Employee("Eve", 40000);
    }
}
